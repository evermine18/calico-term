export interface CustomTag {
  id: string;
  name: string;
  color: string;
}

export interface ContextMenuState {
  x: number;
  y: number;
  tabId: string;
}

export interface TabsSubmenuState {
  x: number;
  y: number;
  tabId: string;
}
